<%= form_with(model: friend) do |form| %>
  <% if friend.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(friend.errors.count, "error") %> prohibited this friend from being saved:</h2>

      <ul>
        <% friend.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.text_field :first_name, class: "form-control", placeholder: 'Nombres' %>
  </div>
  <br>
  <div>
    <%= form.text_field :last_name, class: "form-control", placeholder: 'Apellidos' %>
  </div>
  <br>
  <div>
    <%= form.text_field :email, class: "form-control", placeholder: "Correo electronico" %>
  </div>
  <br>
  <div>
    <%= form.text_field :phone, class: "form-control", placeholder: "Numero de celular" %>
  </div>
  <br>
  <div>
    <%= form.submit class: 'btn btn-outline-danger' %>
  </div>
<% end %>
